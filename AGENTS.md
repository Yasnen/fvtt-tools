# fvtt-tools — AGENTS.md

このファイルをClaude CodeとCodexに共通する作業規則の正本とする。Claude Codeはルートの`CLAUDE.md`から本ファイルを読み込み、Codexは本ファイルを自動検出する。

## プロジェクト概要

- Foundry VTTモジュール開発・ローカライズ向けCLIツール集
- Node.js 20以上、JavaScript（ES Modules）
- 外部ランタイム依存なし
- CLIエントリポイント: `bin/fvtt-tools.mjs`
- 実装: `src/commands/`、共通処理: `src/lib/`
- テスト: `test/`

## 作業規則

- 既存の変更を`git status --short`、`git diff`、`git log --oneline -3`で確認してから編集する。
- 認証情報、端末固有パス、第三者の非公開データをリポジトリへ記録しない。
- 既存CLIの引数、終了コード、標準出力・標準エラーの互換性を、意図なく変更しない。
- ファイルを書き換えるコマンドでは、対象パスを明示的に検証し、利用可能なら先に`--dry-run`で確認する。
- テスト用データは最小限とし、第三者著作物の本文をfixturesへ収録しない。
- 新しい外部依存は、標準モジュールで実現できない理由を確認してから追加する。
- 変更後は`npm test`を実行し、失敗した状態で先へ進まない。
- `git commit`はユーザーから明示的に依頼された場合だけ実行する。
- `git push`、タグ作成、リリース、外部サービスへの書き込みは自動実行しない。
- Claude CodeとCodexは同じworktreeで同時に作業しない。書き手の交代時にGit差分とバックグラウンド処理を確認する。

## コーディング方針

- 既存のJavaScript／ESM構成を維持し、TypeScriptへの移行は別の明示的な作業として扱う。
- Node.js組み込みモジュールは`node:`形式のimportを優先する。
- CLIのエラーは、利用者が原因と対象を特定できるメッセージにする。
- 破壊的なファイル操作には、対象の存在、種別、範囲を検証するガードを設ける。
- バグ修正には、可能な範囲で再現テストを追加する。

