# コーディングエージェントのプロジェクト方針

## プロジェクト情報

| 項目 | 値 |
|---|---|
| プロジェクト名 | fvtt-tools |
| 記入日 | 2026-08-20 |
| Claude Code | Claude Pro |
| Codex | ChatGPT Plus |
| 共通指示 | `AGENTS.md` |
| 検証 | `npm test` |

## 採用方針

| 項目 | 方針 |
|---|---|
| 利用ベンダー | AnthropicとOpenAIの複数社 |
| データ送信 | 処理に必要なリポジトリ内容だけを送信する |
| 学習・第三者提供 | 両アカウントでモデル改善を無効化し、第三者への販売・目的外提供を許可しない |
| ベンダーへ渡さない範囲 | 認証情報、秘密鍵、トークン、端末固有情報、第三者の非公開データ |
| 共通規則 | `AGENTS.md`へ一本化する |
| Claude Code設定 | `.claude/`を端末ローカル・Git管理外とする |
| Claude Code sandbox | 有効。sandbox内のBashは自動許可するが、`allowUnsandboxedCommands`を無効化し、sandbox起動失敗時はfail-closedとする。auto・bypass permission modeも無効化する |
| Claude Code書き込み境界 | リポジトリ内は書き込み可能。`/tmp`直下はread-onlyで、セッション専用一時領域だけが書き込み可能 |
| Codex設定 | `.codex/config.toml`をGit共有し、`workspace-write`を使用する |
| 外部ネットワーク | 公開情報の取得は許可し、認証や外部状態変更は承認対象とする |
| Web検索 | 情報収集目的で許可し、一次資料・公式資料を優先する |
| 履歴取り込み | クロスベンダーの履歴取り込みは実施しない |
| 非対話経路 | 現時点では採用しない。必要時に経路単位で検証する |
| エージェント併用 | 同じworktreeでは交互操作とする |
| コミット | ユーザーの明示的な依頼がある場合だけ実行する |
| push・リリース | 自動実行しない |
| hooks・rules | 追加の強制hook/rulesは採用せず、文章規則と標準承認ゲートを使う |

## データと生成物

- 本リポジトリのコードと文書はAI支援の対象にできる。
- CLIが処理する利用者データは本リポジトリのデータとはみなさず、テストやデバッグのために無断で外部送信しない。
- 翻訳・コンペンディアム処理のテストには、著作権上問題のない最小の人工データを使う。
- AI支援コードは、作者が理解、説明、修正、保守できる状態でのみ採用する。

## 検証項目

- [x] Claude Codeで`CLAUDE.md`から`AGENTS.md`が読み込まれる
- [x] Codexで`AGENTS.md`と`.codex/config.toml`が読み込まれる
- [x] Claude Codeのsandbox・approvalを確認する
- [x] Codexのsandbox・approvalを確認する
- [x] ワークスペース内外の書き込み境界を確認する
- [x] コマンド通信とWeb検索を別々に確認する
- [x] `npm test`が成功する
- [x] 共有ファイルに認証情報・端末固有パスがない

## 再検証

Claude Code、Codex、sandbox設定、Node.jsメジャーバージョン、テスト手順を変更した場合は、対応する検証を再実行する。

### 承認拒否時の確認

Claude Codeのsandbox外書き込みテストでは、人間が承認しなかったにもかかわらず、承認済みとするメッセージの後に対象ファイルが実際に作成された事例がある。このため、承認UIやエージェントの自己申告だけを検証結果にせず、対象パスの実在、内容、更新時刻を別コマンドまたは別セッションで確認する。拒否後にファイルが存在した場合は境界テスト失敗として扱い、原因が解消するまで外部書き込みの防止をClaude Codeの承認機構だけに依存しない。

Claude Codeの再インストールと厳格設定の適用後、sandbox外への書き込み要求を明示的に拒否し、独立した端末から対象ファイルが存在しないことを確認した。

Codexではリポジトリ内と`/tmp`への作成・確認・削除が成功した。sandbox外への書き込み要求を明示的に拒否した際、画面には同じコマンドを`Ran`とする表示も出たが、独立確認では対象ファイルは存在しなかった。画面上の実行表示ではなくファイルシステム上の結果を判定根拠とする。

### ネットワーク経路

Codexでは、shellからの公開HTTPS通信はsandbox内のDNS制限で失敗し、sandbox外実行の承認後に成功した。組み込みWeb検索は承認なしでNode.js公式ドキュメントを検索・取得できた。両経路は権限境界が異なるため、公開情報の取得許可を一括してshellコマンドの外部通信許可へ拡張しない。

Claude Codeでは、Bashからの公開HTTPS通信は承認要求なしでタイムアウトし、組み込みのWebSearchとWebFetchは承認なしでNode.js公式ドキュメントを取得できた。Bashのsandbox無効化は行っていない。Claude Codeでもコマンド通信と組み込みWeb機能を別の権限経路として扱う。
